# AGENTS.md

AIコーディングエージェント（Claude Code / Codex / Cursor / Copilot など）向けの共通ガイドです。
人間の開発者にとっても有効なルールなので、チーム全員がこのファイルに従います。

## プロジェクト概要

- **名称**: AiCT ハッカソン 2026「会津の"おいしい"を、もっとおもしろくする。」
- **テーマ**: 食 × 地域 × AI で会津の食をアップデートする
- **ゴール**: 2026/11/8 の AiCT じものまつりで「人に見せられるもの」を発表する
- **ステータス**: アイデア検討中。何をつくるかは 9/24 の最終決定会で 1 案に決まる
- 詳細は [docs/hackathon-overview.md](docs/hackathon-overview.md) を参照

技術スタックは未決定です。決まり次第、このファイルの「技術スタック」「コマンド」セクションを更新してください。

## 技術スタック

> TODO: 9/28 の開発スタート講座までに決定して記入する

- 言語 / フレームワーク: 未定
- パッケージマネージャ: 未定
- DB / 外部 API: 未定
- デプロイ先: 未定

## コマンド

> TODO: スタック決定後に記入する。エージェントは推測でコマンドを実行しない。

```bash
# セットアップ
# install: (未定)

# 開発サーバー
# dev: (未定)

# テスト
# test: (未定)

# Lint / フォーマット
# lint: (未定)
```

## ディレクトリ構成

```
.
├── AGENTS.md            # このファイル（エージェント共通ルール）
├── CLAUDE.md            # Claude Code 固有の設定（AGENTS.md を import）
├── README.md
├── docs/                # 企画・仕様・議事録などのドキュメント
│   └── hackathon-overview.md
└── .github/             # PR / Issue テンプレート、CI
```

コードを置くディレクトリはスタック決定後に追加します。

## 開発の原則

ハッカソンなので「動くものを短期間でつくる」ことを最優先します。

1. **小さく作って早く見せる**: 完成品より、デモできる状態を優先する
2. **スコープを守る**: 「今回つくる範囲・つくらない範囲」は docs に明記し、範囲外は Issue に積むだけにする
3. **シンプルに**: 過剰な抽象化・将来を見越した汎用化はしない。今必要なものだけを書く
4. **秘密情報をコミットしない**: API キー・トークンは `.env` に置き、`.env.example` にキー名だけを残す
5. **記録を残す**: 決定事項・進捗は Decidim と docs の両方に残す

## コーディング規約

- 既存コードのスタイルに合わせる。新しいスタイルを持ち込む前にチームで合意する
- 変数名・関数名・コミットメッセージは **英語**、UI 文言・ドキュメント・コメントは **日本語** を基本とする
- フォーマッタ / リンタが設定されたら必ず通してからコミットする
- 生成 AI が書いたコードでも、動作確認せずにコミットしない

## Git 運用

### ブランチ

- `main`: 常にデモ可能な状態を保つ。直接 push しない
- 作業ブランチ: `<type>/<short-description>` 例: `feat/recipe-suggest`, `fix/login-redirect`, `docs/idea-vpc`

### コミットメッセージ

[Conventional Commits](https://www.conventionalcommits.org/ja/) に従う。

```
<type>(<scope>): <summary>

<body: なぜこの変更が必要か>
```

主な type: `feat` / `fix` / `docs` / `refactor` / `test` / `chore` / `style` / `ci`

### Pull Request

- `.github/PULL_REQUEST_TEMPLATE.md` に沿って書く
- 1 PR = 1 目的。大きくなりそうなら分割する
- スクリーンショットや動画を添付できるものは添付する（発表資料にも流用できる）
- CI が通り、1 人以上のレビューを受けてからマージする

## エージェントへの指示

- 作業を始める前に `docs/` を読み、現在の企画の方向性を把握する
- スタックやコマンドが「未定」のままなら、勝手に選定・インストールせず、ユーザーに確認する
- ファイルを削除・上書きする前に内容を確認し、意図と異なる場合は報告する
- 破壊的な Git 操作（`push --force`, `reset --hard`, ブランチ削除）は明示的な指示がある場合のみ行う
- 新しい依存を追加する場合は、理由と代替案を PR 本文に書く
- テストが失敗した場合はそのまま報告し、結果を偽らない
- 日本語で応答する
